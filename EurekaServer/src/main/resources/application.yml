spring:
  application:
    name: eureka-server
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    replication:
      enabled: true
  
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF